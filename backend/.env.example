PORT=5000
MONGODB_URI=mongodb://localhost:27017/oreng
FRONTEND_URL=http://localhost:5173

# Admin bootstrap (created on first boot if missing)
ADMIN_EMAIL=admin@oreng.com
ADMIN_PASSWORD=changeme123
ADMIN_NAME=Oreng Admin

# JWT
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=7d

# Sales notifications
SALES_EMAIL=sales@oreng.com

# SMTP (optional — logs to console if unset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Cloudinary CDN Configuration
CLOUDINARY_CLOUD_NAME=uf3q04pk
CLOUDINARY_UPLOAD_PRESET=oreng_admin
